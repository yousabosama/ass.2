#include <stdio.h>
int checkPower(int n)
{
    int p2=1,p3=1;
    while(p2<n) p2*=2;
    while(p3<n) p3*=3;
    if(p2==n) return 1;
    else if(p3==n) return 0;
    else return -1;
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d", checkPower(n));
    return 0;
}
